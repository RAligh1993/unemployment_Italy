# Core
streamlit>=1.36
pandas>=2.2
numpy>=1.26
plotly>=5.20
streamlit==1.37.0

# Modeling
scikit-learn>=1.3
statsmodels>=0.14
shap>=0.46  # optional; heavy; enables SHAP plots on page 7

# NLP / News (optional)
feedparser>=6.0     # RSS
requests>=2.31      # NewsAPI / GDELT / local HTTP LLM
nltk>=3.8           # VADER sentiment (optional)
PyPDF2>=3.0         # PDF text extraction for RAG

# LLM providers (optional for page 6)
openai>=1.30
anthropic>=0.36
google-generativeai>=0.7

# Export / misc
markdown>=3.6       # Markdown → HTML for report builder

# (Linux wheels) If SHAP build fails, try installing these before shap:
# numba>=0.59
# llvmlite>=0.43
